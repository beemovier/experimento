#include <stdio.h> //para scanf e printf, o "f" é de format
int main(){
    printf("hello world!\n"); //pelo visto não pode usar aspas simples
    return(0);
 //gcc hello.c -o "nomedoexecutavel" pra compi'lar
 //""./hello" pra rodar no linux ou só "hello" no cmd
}
